#!/bin/bash

DIRNAME=$(cd `dirname $0` && pwd)
USERNAME=$(<~/.meteor/wishpool.com.username)
PASSWORD=$(<~/.meteor/wishpool.com.password)
HOST="proximus.modulusmongo.net"
PORT="27017"
DB="Y2petuqe"
OUT_DIR="/tmp"

mongo -u $USERNAME -p $PASSWORD $HOST:$PORT/$DB
