template(name="index")
  .container
    .row
      .col-xs-12.col-md-6.col-md-offset-3
        h2 Setup Wishpool on your site
        textarea(rows="4").form-control
          | some code
        .buttons
          button.btn.btn-success(type="button") Copy to clipboard