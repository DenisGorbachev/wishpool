.feedback
  .source-url
    text-decoration underline
    &:hover, &:focus, &:active
      color #333333

.feedback.is-open
  border-left-width 4px

a.feedback.list-group-item
  &:hover, &:focus
    background-color inherit
